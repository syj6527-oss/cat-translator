# v1.4.1 배포 검증 기록

검증일: 2026-09-09

최종 코드로 만든 후보 ZIP을 각각 추출해 3회 검사했습니다. 각 회차 15개 검사 묶음이 모두 통과했으며, 세 회차의 배포 코드 바이트가 동일함을 확인했습니다.

## 패치 결과

- 고속모드에서 중첩 재시도 4회가 가능했던 재현 조건은 이제 총 2회에서 종료합니다. 심각한 오류가 남으면 결과를 억지로 적용하지 않습니다.
- 수용된 72점 결과의 동일 조건 재요청은 캐시로 처리되어 API 호출이 추가되지 않습니다.
- 같은 프로필의 모델·주소 변경, 문맥 A/B 저장, 내부 번역/일반 번역 캐시 분리를 확인했습니다.
- 프로필 중단 시 signal 전달과 즉시 로컬 종료, 늦은 결과 미적용을 확인했습니다. 직접 API는 응답 본문이 멈춰도 시간초과가 작동하며 자동 재시도를 하지 않습니다.
- 동시 요청의 디버그 프롬프트·응답이 섞이지 않습니다. 모든 API 시도의 대기시간을 기록합니다.
- 내부 번역 품질 보정 예제의 프롬프트 길이는 897→1,018자였습니다. 보정 결과를 다음 요청에서 캐시로 재사용했습니다.
- 변경된 메시지 한 개에서 DOM 변경 알림 100묶음이 발생하는 모의 조건은 한 번의 메시지 편집 검사로 합쳐졌습니다.
- 영구 캐시 실패 후 메모리 캐시 저장·조회·핀·삭제 동작을 확인했습니다.
- 단일 길이 수치·단위 불일치 및 명시적 단수 주어의 성별 반전 예제를 검출했습니다. 올바른 3피트/0.91미터 변환과 성별이 불명확한 주어는 오탐하지 않았습니다.

## 검사 묶음

| 검사 | 각 회차 결과 |
|---|---|
| 고속 토글·모델 경로·연결 오류·응답 추출·설정 보존 (215개 검사) | 통과 |
| 사전 팝업·설정 수집 모의 UI (36개 검사) | 통과 |
| 오류 안내·민감정보 가림 (13개 사례) | 통과 |
| 내부 번역 상태·원문/전달문·편집 취소·Enter (68개 검사) | 통과 |
| 공통/서술/대사·기본설정·빈 프롬프트 (43개 검사) | 통과 |
| 배포 파일 구성·버전 (24개 검사) | 통과 |
| 비제미나이 고속·빈 응답·내부 번역 | 통과 |
| 토큰 변형 누수 (38개 검사) | 통과 |
| 구조·병기 변형 행렬 (31개 검사) | 통과 |
| 캐시 왕복: API 1회·캐시 1회·캐시 진행 알림 0회 | 통과 |
| 로그 복사·수동 복사 폴백 | 통과 |
| Response 래퍼 복구: API 추가 호출 없음 | 통과 |
| 매크로·병기 후처리 (8개 검사) | 통과 |
| 신규 요청·캐시·취소 회귀 (11개 시나리오) | 통과 |
| 신규 저장 장애·UI 순회·품질 경계 (19개 검사) | 통과 |

검사 개수는 각각의 묶음 안에서 집계한 값이며 서로 중복되는 확인 범위가 있습니다.

## 측정 범위와 설치

검사는 실제 배포 소스와 모의 API·DOM을 사용했습니다. 실제 제공업체 호출이나 실제 Android/SillyTavern 브라우저 성능 측정은 하지 않았습니다. 모델 속도가 몇 퍼센트 빨라졌다는 실측 결과는 아닙니다. 제공업체·서버가 취소 신호를 무시하면 서버 작업은 계속될 수 있습니다.

기존 설정과 번역 이력은 유지합니다. 모델·문맥 캐시 키를 보강했으므로 업데이트 직후 기존 자동 캐시는 한 번 다시 번역될 수 있습니다. 고속모드는 선택 사항이며 기존 설정값을 유지합니다. 기존 확장 폴더에 전체 파일을 업데이트하고 새로고침한 후 디버그 버전 1.4.1을 확인하세요. 새 request.js도 함께 설치되어야 합니다.

성별·숫자·맞춤법·의미 품질의 전면 보장은 아닙니다. 단위 검사도 단일 길이 표현 등 판별 가능한 제한된 사례만 다룹니다.
